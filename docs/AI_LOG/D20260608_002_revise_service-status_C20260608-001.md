# AI_LOG セッション D20260608_002 — /flow:revise service-status C20260608-001

**実行日時**: 2026-06-08 08:45 (+09:00)
**コマンド**: /flow:revise service-status C20260608-001 --from-claim=C20260608-001 --slug=realtime-refresh-gap
**実行者**: Claude (Opus 4.8) + seiji
**状態**: 設計完了 → tdd へ自動進行
**含まれる decision**: 改修要望取得 / Read スコープ / 改修方針 / TTL / 最終同期日時表示 / 表示形式
**依存**: claim D20260608_001 (C20260608-001 判定 = 仕様検討漏れ) / feature D20260527_014 (service-status SPEC 初版 UC-S5 cron N分間隔)

> **サマリ**: claim C20260608-001（HUB 3件 / shipyard 2件・naze-bako 欠落）の revise。日次 cron は Vercel Hobby 制約のため、鮮度担保を cron から**リクエスト駆動 read-through cache** に移行（cron 非依存・追加コストなし）。ユーザー指示で TTL=**1時間**（最終同期日時=既存 fetchedAt 流用、スキーマ変更なし）、さらに**最終同期日時を「{日時}現在」形式で UI 表示**を追加。後方互換・migration 不要。4 文書生成、3 Phase 分割。

---

## Decisions

- **D20260608-009 改修要望取得** [explicit-choice]: claim TRIAGE ハンドオフ + ユーザー追加指示（TTL 1h / 最終同期日時保存 / 表示 / 「現在」形式）。
- **D20260608-010 Read スコープ** [auto-recommended]: lib/hub/{cache,client,contract}.ts, lib/db/repositories/statusCache.ts, lib/service-status/{load,api}.ts, app/api/{services,cron}, app/page.tsx, features/service-status/StatusList.tsx, vercel.json, SPEC（service-status/hub-client）。claim フェーズで大半 Read 済み。
- **D20260608-011 改修方針（更新戦略）** [auto-recommended]: TRIAGE 案 A = **read-through cache（TTL + graceful fallback + in-memory throttle）**、日次 cron は backstop 維持。理由: Vercel Hobby cron 制限は cron スケジュールにのみ適用、リクエスト処理内 fetch は対象外 → cron 非依存で鮮度実現、追加インフラ/コストなし、concept 軽量方針と整合。depends_on: D20260608-007(claim 判定)。
- **D20260608-012 TTL 既定値** [explicit-choice]: ユーザー指示「最終同期日時を保存し訪問者リクエストから1時間以上経過で refresh」→ **TTL=3600s（1時間）**。最終同期日時は既存 `service_status_cache.fetchedAt` 流用（マイグレーション不要）。env `STATUS_REFRESH_TTL_SEC` で調整可。論点-001 解決。
- **D20260608-013 最終同期日時表示** [explicit-choice]: ユーザー指示「最終同期日時を表示する」→ StatusList に syncedAt prop 追加 + /api/services に top-level syncedAt（additive 後方互換）。0 件時非表示。
- **D20260608-014 表示形式** [explicit-choice]: ユーザー指示「日時現在 という表示」→ **「{日時}現在」**（例「2026年6月8日 8:30 現在」、JST）。i18n カタログ無し PJ のため JP ハードコード、最終文言は /flow:wording で調整余地。
- **D20260608-015 後方互換/migration/ロールバック** [auto-recommended]: 互換維持（additive・スキーマ無変更）/ migration 不要 / コード revert でロールバック / 一括リリース（フラグ不要）。

## 生成・更新ファイル
- docs/service-status/revise_C20260608-001_20260608_realtime-refresh-gap/{001_REVISE_SPEC,002_REVISE_PLAN,003_REVISE_UNIT_TEST,004_REVISE_E2E_TEST,README,INDEX}.md
- docs/service-status/INDEX.md（サブフォルダ表に claim + revise 行追加）
- docs/AI_LOG/D20260608_002_revise_service-status_C20260608-001.md（本ファイル）

## 学習・改善
- 「プラットフォーム制約（Vercel Hobby cron 日次）で SPEC の意図（N分間隔リアルタイム）が潰れた」ケースの定石 = 制約のある仕組み（cron）への依存をやめ、別経路（リクエスト駆動 read-through）に鮮度担保を移す。制約を「破る」のではなく「回避する」設計。
