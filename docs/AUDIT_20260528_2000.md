# 設計書類整合性監査レポート

**監査日時**: 2026-05-28 20:00 (+09:00)
**監査実施者**: Claude (Opus 4.7) + seiji
**対象**: shipyard (公開ショーケース + AI コンサル lead-gen LP)
**scope**: full
**実行カテゴリ**: #1 構造 / #2 依存 / #3 論点ステータス / #4 観点反映 (required_signals AND マッチ含む) / #5 AI_LOG 整合性 (枠組み) / #6 PREREQUISITES 整合性 (枠組み) / #7 収益 (枠組み) / #8 テストカバレッジ (枠組み) / #9 法務 (枠組み)
**前回監査との差分**: AUDIT_20260528_1913.md (commit `b885cfd`) → 本回 (commit `40715ff`)、5 commits 蓄積 (revise 設計 + tdd Phase 1+2 + reports)
**起動文脈**: /flow:auto §3.0c release-pre 必須監査ハードゲート発火 (P4.7 評価直前、最新 AUDIT 参照 commit ≠ HEAD、CF-20260528-009)

## 1. サマリ

- **総検出問題**: 6 件
- **Critical**: 0 件
- **High**: 1 件 (AUDIT-perspective-001 O48 v2 契約 drift = HUB_SHARED_SECRET 旧名 + iconUrl 未追従)
- **Medium**: 3 件 (SCENARIO §5 stale [3 連続常習化] / DOC_MAP.md stale / PREREQUISITES §1 HUB_SHARED_SECRET 旧名)
- **Low**: 2 件 (INDEX.md 状態列細部 stale / 解消済論点 §8 残存 3 件)
- **Info**: 0 件
- **§8 自動登録**: 0 件 (`--auto-register-issues` 未指定、ただし AUDIT-perspective-001 は [論点-008] 登録推奨)
- **未実装カテゴリ**: #5 AI_LOG 整合性 / #6 PREREQUISITES 整合性 / #7 収益 / #8 テストカバレッジ / #9 法務 (枠組みのみ、本回 light スキャンで主要 drift のみ拾い)
- **抑制された検出**: 0 件 (`.audit-suppressions.yml` 未作成)
- **本回新規追加した抑制**: 0 件
- **抑制期限切れ**: 0 件
- **履歴トレンド方向**: **悪化** (Critical 0 / High 0→1、Medium 1→3 = O48 v2 drift 新規検出 + SCENARIO §5 stale 再発)
- **常習化検出**: **1 件** (AUDIT-structure-001 SCENARIO §5 stale が直近 3 回連続検出 = 構造的問題、要対応)

## 2. カテゴリ別サマリ

| # | カテゴリ | 実行状態 | 検出件数 (Critical / High / Medium / Low) | 主要問題 |
|---|---|---|---|---|
| #1 | 構造的整合性 | ✅ 実行 | 0 / 0 / 2 / 1 | SCENARIO §5 stale (3 連続常習化) + DOC_MAP.md stale |
| #2 | 依存整合性 | ✅ 実行 | 0 / 0 / 0 / 0 | 循環依存なし、全基盤設計済 ✓ |
| #3 | 論点ステータス | ✅ 実行 | 0 / 0 / 0 / 1 | 解消済 3 論点 §8 残存 (§7 移動推奨) |
| #4 | 観点反映 (required_signals AND マッチ含む) | ✅ 実行 | 0 / **1** / 0 / 0 | **O48 v2 契約 drift = HUB_SHARED_SECRET 旧名 + iconUrl 未追従** |
| #5 | AI_LOG 整合性 | ⏳ 枠組み | (本回スキャン) 0 / 0 / 0 / 0 | release セッション D007 進行中継続 = 正常 |
| #6 | PREREQUISITES 整合性 | ⏳ 枠組み (主要 drift のみ) | 0 / 0 / 1 / 0 | HUB_SHARED_SECRET (旧名) 残存 = #4 連動 |
| #7 | 収益モデル整合性 | ⏳ 枠組み | 0 / 0 / 0 / 0 | 課金なし整合 ✓ |
| #8 | テスト計画カバレッジ | ⏳ 枠組み | 0 / 0 / 0 / 0 | 174 tests GREEN (E2E は [論点-005] Playwright bootstrap 未完了) |
| #9 | 法務書類整合性 | ⏳ 枠組み | 0 / 0 / 0 / 0 | プラポリ + 利用規約 ✓、特商法不要 (課金なし) |

## 3. 詳細検出結果 (severity 降順)

### 3.1 Critical
- なし

### 3.2 High

#### [AUDIT-perspective-001] O48 service-info v2 (favicon-projection) 契約 drift

- **カテゴリ**: #4 観点反映 (required_signals AND マッチ、CF-20260528-010 + CF-20260528-019)
- **検出根拠**:
  - perspectives.md O48 `required_signals` (現契約の必須キー、AND マッチで全揃いを要求):
    1. `HUB_SERVICE_INFO_SECRET` (全サービス共通シークレット、旧 per-service `HUB_SHARED_SECRET` 廃止)
    2. `/api/hub/service-info` (現契約 endpoint path)
    3. `iconUrl` (v2 favicon-projection、producer-side handler に iconUrl 露出必須)
  - shipyard コードベース実 grep 結果:
    1. `HUB_SERVICE_INFO_SECRET`: **0 件** ❌ (`lib/hub/service-info.ts:26` + `app/api/hub/service-info/route.ts:9` + `.env.example:29` + `.env.development.example:51` + `.env.production.example:45` すべて旧名 `HUB_SHARED_SECRET`)
    2. `/api/hub/service-info`: 1 件 ✓ (`app/api/hub/service-info/route.ts:4` コメント)
    3. `iconUrl`: 1 件のみ ✓ (service-status consumer 側 `docs/service-status/revise_service-icons_*/`、**producer 側 `lib/hub/service-info.ts` ServiceInfo interface に未追加** = consumer-only でレスポンスに iconUrl が出ない)
  - **2/3 必須キー不足 = 契約 drift**
- **影響範囲**:
  - `lib/hub/service-info.ts` (ServiceInfo interface + serviceInfoPayload + isAuthorizedHub)
  - `app/api/hub/service-info/route.ts` (env var 読み出し)
  - `.env.example` + `.env.development.example` + `.env.production.example` (env 名)
  - `lib/hub/service-info.test.ts` (test 名 + 検証)
  - `docs/PREREQUISITES.md` §1 (HUB_SHARED_SECRET 行 → HUB_SERVICE_INFO_SECRET)
  - `docs/concept.md` §6 (HUB_SHARED_SECRET の言及 → HUB_SERVICE_INFO_SECRET)
- **既存実装の重要事実**: `app/icon.svg` は配線済 (O56 favicon 解消、D014)。よって iconUrl 露出は容易: `https://shipyard.<domain>/icon.svg` (Next.js が自動 serve) を service-info レスポンスに含めるだけ
- **推奨修正アクション**: `/flow:revise _shared/hub-client` (または `<root>` で対象自動推定) で v2 contract retrofit:
  1. **env rename**: `HUB_SHARED_SECRET` → `HUB_SERVICE_INFO_SECRET` (3 ファイル `.env.*example` + コード参照 2 箇所、test 1 箇所、PREREQUISITES + concept §6)
  2. **iconUrl 追加**: `ServiceInfo` interface に `iconUrl?: string` 追加、`serviceInfoPayload()` 引数に `siteUrl?: string` 追加して `iconUrl: siteUrl + "/icon.svg"` 生成 (HTTPS + 公開ホスト + 1024 chars 以内、SSRF 予防)
  3. **schemaVersion bump**: 1 → 2 (v2 推奨)
  4. **test 拡張**: `lib/hub/service-info.test.ts` に iconUrl 検証 + HUB_SERVICE_INFO_SECRET 認証 test 追加
- **§8 登録**: [論点-008] として登録推奨 (本コマンドは `--auto-register-issues` 未指定なので登録せず、推奨のみ)
- **判断期限**: **Release 前必須** (HUB が pull する v2 契約に追従しないと HUB ダッシュボードでアイコン表示されない + 旧シークレット名は v2 contract 違反)

### 3.3 Medium

#### [AUDIT-structure-001] SCENARIO.md §5 stale (3 回連続常習化)

- **カテゴリ**: #1 構造的整合性 (常習化検出 = AUDIT_20260528_1640 / 1913 / 2000 で 3 回連続)
- **検出根拠**:
  - SCENARIO.md §5 「Phase 2 動作確認 Step 4-5 残」記述 ← **本セッションでユーザー報告「contact + email 動作確認済み」により完了** (反映漏れ)
  - SCENARIO.md §5 「[論点-005/007] §8 未登録 (Low reconcile、本反復で実施候補)」 ← **§8 grep 結果: 両論点とも既登録** (stale 記述)
  - SCENARIO.md §5 「本日 27 commits」 ← **本日 32 commits** (D018 inquiry revise tdd + 102 reports + 本セッション開始までの 5 commits 未反映)
  - SCENARIO.md §5 「最終更新セッション D016 後の §3.0c シューティング reconcile」 ← **以降に D017 revise + D018 tdd が完遂** (反映漏れ)
- **常習化原因 (3 回連続検出からの推論)**: 機能完遂直後の §5 更新が手動依存。各 flow コマンドの Resume Contract §0.1 で「次アクション提示」は走るが、SCENARIO §5 への書き戻し義務が無い構造的問題
- **推奨修正アクション**:
  1. **即時**: `/flow:scenario --update` で §5 全面 refresh
  2. **構造的**: flow-suite で「tdd / revise / e2e 等が完了したとき AUDIT_REQUEST.md または .scenario-update-needed marker を書く」hook を検討 (CF-014/015/017 と同系統の補強候補)
- **§8 登録**: 不要 (運用パターン課題、flow-suite 側で対応)

#### [AUDIT-structure-002] DOC_MAP.md stale

- **カテゴリ**: #1 構造的整合性
- **検出根拠**:
  - `docs/DOC_MAP.md` 冒頭: 最終更新 2026-05-28 12:15、最新コマンド = D20260528_002 ← **以降に D003〜D018 + 本セッションまで 16 セッション進行**
  - §1 status: 「企画（concept 更新 — メッセージング転換 2026-05-28）」 ← **実際 Phase 4 公開準備 (Release gate Phase 1+2 完了 + Phase 3 デプロイ残)**
  - §2 機能フォルダ全件「設計待ち」 ← **INDEX.md では unit 実装完了**
  - §3 横断フォルダ全件「設計待ち」 ← **INDEX.md では実装完了**
  - §4 最新セッション「D20260527_001_concept_initial」 ← **実際 D20260528_018**
  - §4 Open 論点 1 件 ← **実際 [論点-005] のみ open** (件数は同じ偶然合致だが、001/006/007 resolved 反映漏れ)
- **推奨修正アクション**: `/flow:concept` UPDATE モード (DOC_MAP は concept が auto-generate 範囲) または flow-suite の DOC_MAP 自動更新

#### [AUDIT-prereq-001] PREREQUISITES.md §1 HUB_SHARED_SECRET (旧名) 残存

- **カテゴリ**: #6 PREREQUISITES 整合性 (AUDIT-perspective-001 の波及)
- **検出根拠**: PREREQUISITES.md §1 に HUB 連携キーとして `HUB_STATUS_URL` のみ列挙、service-info 用 secret は未列挙。コード上 `HUB_SHARED_SECRET` が使用されているが PREREQUISITES に明示なし
- **推奨修正アクション**: AUDIT-perspective-001 retrofit と同 commit で `HUB_SERVICE_INFO_SECRET` を §1 に追加 (用途: O48 service-info Bearer 認証、取得方法: openssl rand -hex 32 を seiji が生成 → HUB 側に同値共有)

### 3.4 Low

#### [AUDIT-issue-001] 解消済 3 論点 §8 残存 ([論点-001] [論点-006] [論点-007])

- **カテゴリ**: #3 論点ステータス
- **検出根拠**:
  - [論点-001] status=resolved (2026-05-28、D20260528_007) ← §8 残存
  - [論点-006] status=resolved (2026-05-28、D20260528_017) ← §8 残存
  - [論点-007] status=resolved (2026-05-28、D20260528_013) ← §8 残存
- **推奨修正アクション**: `/flow:concept` UPDATE で §7 決定事項ログへ移動 (bookkeeping、release 前後で実施)
- **影響**: 軽微 (resolved 注記は明示済、誤解は生まない)

#### [AUDIT-structure-003] INDEX.md 状態列細部 stale

- **カテゴリ**: #1 構造的整合性
- **検出根拠**:
  - service-status: 「unit 実装完了 (7件 GREEN, E2E 待ち, revise#1 設計完了)」 ← **revise#1 tdd 完遂 (D015)、11 件追加 (7→18件 GREEN)** 反映漏れ
  - inquiry: 「unit 実装完了 (14件 GREEN, E2E 待ち)」 ← **revise#1 tdd 完遂 (D018)、2 件追加 (14→16件 GREEN)** 反映漏れ
  - landing: 「unit 実装完了 (10件 GREEN, E2E 待ち, revise#1 unit 完了)」 ← 既に revise#1 反映済、ただし wording gate D006 通過の記述なし
- **推奨修正アクション**: `/flow:concept` UPDATE (auto-generate 範囲) または各 INDEX を手動 reconcile

## 4. 推奨アクション一覧 (severity 降順、実行コマンド込み、シューティングリスト)

| # | severity | アクション | 実行コマンド | Class |
|---|---|---|---|---|
| 1 | **High** | O48 v2 contract retrofit (`HUB_SHARED_SECRET` → `HUB_SERVICE_INFO_SECRET` + `iconUrl` 追加 + `schemaVersion=2`) | **`/flow:revise _shared/hub-client`** (推定 target、または `<root>` で自動推定) | **A** (auto-execute、コード+テスト+ドキュメント、git tracked、Release 前必須) |
| 2 | Medium | SCENARIO.md §5 全面 refresh | **`/flow:scenario --update`** | A (auto-execute) |
| 3 | Medium | DOC_MAP.md stale 修正 | **`/flow:concept` UPDATE** (DOC_MAP は concept auto-generate 範囲) | A (auto-execute) |
| 4 | Medium | PREREQUISITES.md §1 に `HUB_SERVICE_INFO_SECRET` 追加 | Item 1 retrofit と同 commit で同時更新 | A |
| 5 | Low | 解消済 3 論点 §8 → §7 移動 ([論点-001/006/007]) | `/flow:concept` UPDATE (bookkeeping) | A (release 前後) |
| 6 | Low | INDEX.md 状態列 reconcile | `/flow:concept` UPDATE (auto-generate) | A (release 前後) |

## 5. 次回監査推奨タイミング

- **High 解消後 (#1 O48 retrofit 完了後)**: 即時再監査推奨 (release-pre 監査として再実行、commit ≠ HEAD なら必須、CF-20260528-009)
- **Release Phase 3 (Vercel デプロイ) 直前**: full scope で最終チェック (本回が release-pre 監査として該当、retrofit 完了後に再回す)

## 6. 監査履歴 (直近 5 件) + トレンド

### 6.1 履歴テーブル

| 日時 | scope | Critical | High | Medium | Low | 抑制件数 | コメント |
|---|---|---|---|---|---|---|---|
| 2026-05-28 20:00 (本回) | full | 0 | **1** | 3 | 2 | 0 | **悪化** (O48 v2 drift 検出、SCENARIO stale 3 連続常習化) |
| 2026-05-28 19:13 | standard | 0 | 0 | 1 | 3 | 0 | 改善 (O56 favicon 解消) |
| 2026-05-28 16:40 | standard | 0 | 0 | 1 | 2 | 0 | 改善 (High 1→0、O48 service-info 配線完了) |
| 2026-05-27 17:00 | standard | 0 | 1 | (未集計) | (未集計) | 0 | O48 service-info 未実装 (High)、Phase 3 unit 完了時点 |

### 6.2 トレンド判定

- **方向**: **悪化** (前回 0/0/1/3 → 本回 0/1/3/2、High +1 + Medium +2)
- **判定根拠**:
  - 新規 High 検出: AUDIT-perspective-001 O48 v2 (favicon-projection) 契約 drift = CF-20260528-019 (perspectives.md 学習ログ 2026-05-28) で v2 改訂された後、shipyard が retrofit 未実施 + `HUB_SHARED_SECRET` 旧名残存
  - 過去の AUDIT (1640 / 1913) で「O48 配線済」と判定したのは `code_signals` の OR マッチで `HUB_SHARED_SECRET` (旧名) が含まれていたため (broad match)。本回 `required_signals` AND マッチで初めて drift 検出
- **常習化検出 (同 rule_id が 3 回以上連続)**:
  - **AUDIT-structure-001 (SCENARIO §5 stale)**: AUDIT_20260528_1640 / 1913 / 2000 で **3 回連続検出** → **構造的問題のフラグ**、運用パターンの根本対応推奨 (flow-suite で hook 検討 / 各 flow コマンドの §5 自動 update / `.audit-suppressions.yml` 期限付き登録のいずれか)

### 6.3 改善ハイライト (前回比)

- **解消された問題**:
  - [論点-007] resolved (D013 spec-review で確定、§8 status 反映済)
  - [論点-006] resolved (D017 revise + D018 tdd で reconcile 完遂)
- **新規発生した問題**:
  - **AUDIT-perspective-001 O48 v2 contract drift** (High) — v2 改訂 (CF-20260528-019) 後の retrofit 未実施
  - AUDIT-structure-002 DOC_MAP stale (Medium) — 12:15 以降の 16 セッション未反映
  - AUDIT-prereq-001 PREREQUISITES §1 (Medium) — #4 drift の波及
  - AUDIT-issue-001 解消済 3 論点 §8 残存 (Low)

## 7. 抑制サマリ (`.audit-suppressions.yml`)

### 7.1 本回適用された抑制

| rule_id | target | 理由 | 追加日 | 有効期限 |
|---|---|---|---|---|
| (該当なし、ファイル未作成) |

### 7.2 本回新規追加した抑制 (Step 1.2 経由)

| rule_id | target | 理由 | 期限 | 追加根拠 |
|---|---|---|---|---|
| (該当なし、--auto-pick で自動進行のため Step 1.2 対話なし) |

### 7.3 期限切れ (本回で検出復活)

| rule_id | target | 抑制期間 | 復活した検出の severity |
|---|---|---|---|
| (該当なし、抑制ファイル未作成のため) |

## 8. 結論 + 次アクション (Release gate との関係)

**Release gate (P4.7) Phase 3 デプロイへ進む前に、High 1 件の reconcile が必要**:

1. **[High] O48 v2 contract retrofit** = `/flow:revise _shared/hub-client` (Class A、auto-execute):
   - `HUB_SHARED_SECRET` → `HUB_SERVICE_INFO_SECRET` rename (6 箇所: コード 2 + env 3 + test 1)
   - `iconUrl` 追加 (ServiceInfo interface + serviceInfoPayload)
   - `schemaVersion=2` bump
   - test 拡張
   - PREREQUISITES §1 + concept §6 同 commit 更新
   - **release-pre 必須監査ハードゲートの再実行も必要** (CF-20260528-009)
2. **[Medium x3 / Low x2] bookkeeping**: SCENARIO/DOC_MAP/INDEX/§8 整理 (release Phase 3 デプロイ前 or 後、優先度低)

→ /flow:auto §3.0c drift シューティングが本 finding を撃ち落として fresh になったら P4.7 Release gate Phase 3 デプロイへ合流。
