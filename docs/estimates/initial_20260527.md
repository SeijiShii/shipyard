---
generated_at: 2026-05-27 14:15
generator: /flow:estimate
context: whole
source_input: ./docs/concept.md
interpreted_as:
  feature: null
  phase: rough
  source: autodetect
phase: rough
confidence_band:
  ai_impl: "±300%"
  human_bottleneck: "±50%"
  ai_tokens: "±50%"
nfr_profile:
  scale: low
  throughput: low
  latency: standard
  availability: 99
  source: explicit
calibration:
  global_metrics: "empty (0 sessions) — default coefficients"
  pj_internal: "none (fresh PJ)"
  weight: "global 100% (no data) → defaults"
  note: "metrics 蓄積後、最初の 1 feature 完了直後に refined 再校正予定 (SCENARIO Phase 2)"
summary:
  min:
    total_files: 28
    total_lines: 2600
    human_hours: 6
    ai_tokens_total_K: 400
  standard:
    total_files: 60
    total_lines: 6200
    human_hours: 16
    ai_tokens_total_K: 850
  full:
    total_files: 120
    total_lines: 13000
    human_hours: 36
    ai_tokens_total_K: 1800
---

# 見積もり: shipyard（全体・初回 / rough）

> **phase=rough（concept §1.3 ベース、SPEC 前）**。AI-impl は ±300% の広いバンド。最初の 1 feature 設計完了直後に metrics を取り込んで refined 再校正する（SCENARIO Phase 2、2 タイミング標準）。

## サマリ表

| スコープ | ファイル数 | コード行数 (logic+test) | 人間時間 | AI 推論トークン (設計+実装) |
|---|---|---|---|---|
| Minimum | 28 | 2,600 | 6h | 設計 180K / 実装 220K = 計 400K (±50%) |
| Standard | 60 | 6,200 | 16h | 設計 280K / 実装 570K = 計 850K (±50%) |
| Full | 120 | 13,000 | 36h | 設計 420K / 実装 1,380K = 計 1,800K (±50%) |

> **confidence band**: AI-impl ±300%（rough）/ Human-bottleneck ±50% / AI 推論トークン ±50%
> **NFR**: scale=low, throughput=low, latency=standard, availability=99（個人ポートフォリオ + SNS スパイク、HUB ダウン時 graceful）
> **NFR 倍率合計**: 0.7(scale) × 0.8(throughput) × 1.0(latency) × 1.0(avail) ≈ 0.56x（軽量寄り）
> **金額換算**: コード行数 × AI コーディングレート、トークン × モデル単価（その時期で要確認）で利用側が算出
> **セキュリティ要件の織り込み**: concept §3.7（PII scrub / IDOR / XSS / 不可視スパム）を inquiry / admin / _shared/spam の Std に織り込み済み

## 1. 機能フォルダ別ブレークダウン（Standard 中心）

### docs/landing/ — LP
| task_type | classification | Std files | Std lines |
|---|---|---|---|
| ui_code | ai_impl | 3 | 350 |
| unit_test | ai_impl | 1 | 100 |
| **小計** | | **4** | **450** |
**根拠**: トップ + hero/価値/コンサル打ち出しセクション。動的 OG 画像は _shared/seo に計上。コピーは O38/O42 で別途 `/flow:wording`。

### docs/service-status/ — 稼働一覧
| task_type | classification | Std files | Std lines |
|---|---|---|---|
| ui_code | ai_impl | 2 | 280 |
| api_code（/api/services 配信 + cron refresh） | ai_impl | 2 | 200 |
| unit_test / e2e_auto | ai_impl | 2 | 200 |
| **小計** | | **6** | **680** |
**根拠**: HUB fetch + キャッシュ表示 + リンク。fetch/cache 本体は _shared/hub-client。HUB ダウン時 graceful（last_checked_at 表示）。

### docs/inquiry/ — 問い合わせスレッド（最重量）
| task_type | classification | Std files | Std lines |
|---|---|---|---|
| ui_code（フォーム + スレッド表示 + 追記） | ai_impl | 3 | 380 |
| api_code（POST /api/inquiry + reply、Zod + spam 統合） | ai_impl | 2 | 320 |
| unit_test / e2e_auto | ai_impl | 2 | 300 |
| **小計** | | **7** | **1,000** |
**根拠**: §3.7 SEC-002（token IDOR 検証）+ SEC-003（Zod + XSS 防止）+ 不可視スパム統合（_shared/spam 呼び出し）。トークン URL + localStorage 復帰。

### docs/admin/ — 運用者コンソール
| task_type | classification | Std files | Std lines |
|---|---|---|---|
| ui_code（スレッド一覧 + 返信） | ai_impl | 3 | 320 |
| api_code（返信 API、Clerk guard） | ai_impl | 1 | 120 |
| unit_test | ai_impl | 1 | 130 |
| **小計** | | **5** | **570** |
**根拠**: Clerk gate（_shared/auth）+ allowlist。返信→Resend 通知（_shared/email）。

### docs/legal/ — 法務ページ
| task_type | classification | Std files | Std lines |
|---|---|---|---|
| doc/ui_code（privacy + terms） | ai_impl | 2 | 200 |
| **小計** | | **2** | **200** |
**根拠**: 静的 2 ページ。テンプレ + 自前ドラフト。特商法は不要（課金なし）。

**機能小計（Std）**: 24 files / 2,900 lines

## 2. 横断フォルダ別ブレークダウン（全て is_new=true、greenfield）

| 横断 | 主な内容 | Std files | Std lines |
|---|---|---|---|
| _shared/db | Drizzle schema 5 表 + migration + client | 3 | 280 |
| _shared/ui | shadcn/ui setup + theme + 共通部品 | 6 | 400 |
| _shared/seo | meta + JSON-LD + sitemap + 動的 OG | 4 | 380 |
| _shared/hub-client | contract 型 + fetch + cache + fallback + mock | 4 | 380 |
| _shared/email | Resend wrapper + テンプレ 2 | 3 | 240 |
| _shared/spam | Turnstile + honeypot + timing + rate limit + MX/使い捨て | 5 | 520 |
| _shared/auth | Clerk setup + allowlist + admin middleware | 3 | 200 |
| **横断小計（Std）** | | **28** | **2,400** |
**根拠**: _shared/spam が最重（不可視スタック 5 要素、§3.7 SEC 起点）。hub-client は HUB 未実装時のモック contract（[論点-001]）を含む。全て新規のため全量計上（二重計上なし）。

## 3. 基本部分（12 項目、greenfield 分のみ）

| 項目 | Std 計上 | 備考 |
|---|---|---|
| プロジェクト初期化 | 2 files / 150 | Next.js + TS + Tailwind + Drizzle 設定 |
| CI/CD | 2 files / 120 | GitHub Actions（lint/typecheck/test）+ Vercel preview |
| インフラ・デプロイ | 1 file / 60 | vercel.json + cron 設定（IaC 不要、PaaS 完結） |
| 監視 | 1 file / 50 | Sentry init + beforeSend PII scrub（§3.7 SEC-001） |
| その他（認証/ログ/i18n 等） | _shared に計上済 / i18n なし | — |
| **基本小計（Std）** | **6 files / 380 lines** | README は作成済（計上外） |

## 4. NFR 倍率の効き
| NFR 軸 | 値 | 倍率 |
|---|---|---|
| scale | low | 0.7x |
| throughput | low | 0.8x |
| latency | standard | 1.0x |
| availability | 99 | 1.0x |
| **合計** | | **≈0.56x（軽量寄り、個人ポートフォリオ規模）** |

## 5. 根拠サマリ
1. **inquiry が単一最大ドメイン**（thread + spam + email + IDOR/XSS 対策）。Std で機能の約 1/3 の行数。
2. **_shared/spam の不可視スタック**（5 要素）が横断で最重。concept §3.7 SEC-002/003 と D20260527-005 起点。
3. **hub-client はモック contract 込み**で開始（[論点-001] HUB 未実装でも開発継続可）。
4. **E2E は Playwright で自動化基本**（O33）。人力 E2E はデプロイ前のスマホ実機目視のみ（`/flow:release`）。
5. **人間時間 16h（Std）の内訳**: アカウント設定（Neon/Clerk/Resend/Turnstile）+ DNS サブドメ + 法務コピー確認 + 視覚デザインレビュー + デプロイ実機確認。コーディングは AI-impl。

## 6. 二重計上回避
全横断 `is_new=true`（greenfield）のため二重計上なし。次回 feature 単位見積もり時は、横断を再計上しないこと。

## 7. AI 推論トークン内訳（rough、§11.5.2 経験則）
- **設計（Std ~280K）**: concept NEW 13K + secure 11K + estimate ×2 16K + design 15K + feature ×12（~120K）+ spec-review ×5（~50K）+ 予備 revise/fix（~55K）
- **実装（Std ~570K）**: tdd ×12 + e2e。code_lines 6,200 × tokens/line 1.5 × loop 2 ＋ 各セッション context。±50%
- **注**: global-metrics 空のため経験則デフォルト。最初の 1 feature 完了後に実測で refined 再校正（精度向上）。

> **参考単価（2026-05 時点想定、要確認）**: Haiku 4.5 in$0.80/out$4、Sonnet 4.6 in$3/out$15、Opus 4.7 in$15/out$75（per 1M tokens）
