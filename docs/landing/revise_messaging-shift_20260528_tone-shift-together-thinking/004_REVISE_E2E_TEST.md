# landing E2E テスト計画 (メッセージング転換 — 「共に考える相談相手」スタンス)

> **入力**: [`./001_REVISE_SPEC.md`](./001_REVISE_SPEC.md), [`../../concept.md`](../../concept.md) §1.1, [`../004_landing_E2E_TEST.md`](../004_landing_E2E_TEST.md)
> **最終更新**: 2026-05-28

---

## 1. 変更 UC シナリオ

### UC-L1: トップで「何のサイトか」「どんなスタンスか」を理解する (O41 強化)

| シナリオ ID | 前提 | 操作ステップ | 期待結果 |
|---|---|---|---|
| L1-S2' (入口理解 O41、改修版) | 初見訪問者、稼働一覧あり | `/` を開いてファーストビュー (and below the fold まで visible 状態に) | (a) ヒーローで「何のサイトか」が分かる (リード文に「動いているサービス」「個人開発」等のキーワード) AND (b) ConsultPitch (または Hero) で **スタンスキーワード** 「共に考え / 共に悩 / 正解の見えない / 絶対の正解 / これからを共に」のうち**少なくとも 2 種類**が DOM に存在する (UC-L1 出力「どんなスタンスで関わるか」が伝わる保証) |
| L1-S4 (アンチパターン検出、新規) | 同上 | `/` を開く | DOM テキスト全体に「成功させましょう / 成功をお約束 / 必ず / 絶対に成功 / 今すぐ / 急いで / の秘訣」等のアンチパターン NG キーワードが**1 つも含まれない** (charter §2.2 + SPEC §7.5 トーン (2) を E2E でも担保) |

### UC-L2: スタンスに共感して問い合わせへ進む

| シナリオ ID | 前提 | 操作ステップ | 期待結果 |
|---|---|---|---|
| L2-S1 | — | 「ご相談はこちら」CTA をクリック | `/contact` へ遷移 (**変更なし** — 既存 L2-S1 を維持。CTA テキスト「ご相談はこちら」も維持) |
| L2-S2 (CTA 控えめトーン検出、新規) | — | Hero / ConsultPitch 内の CTA 要素を全て取得 | 各 CTA テキストが**短文** (例えば 12 文字以内) かつ煽り NG キーワード非含 (「いますぐ」「お得」「限定」等)。controlled / 控えめトーンが UI 上で機械的に担保される |

## 2. リグレッションシナリオ (既存 UC、重要度高)

| UC | シナリオ ID | 確認観点 |
|---|---|---|
| UC-L1 (構造) | L1-S1 (happy) | ヒーロー + 稼働一覧 + コンサル (ConsultPitch) + CTA がレンダリングされる (構造維持、文言は新版) — **既存 L1-S1 を維持** |
| UC-L1 (graceful) | L1-S3 (edge、稼働一覧 0/失敗) | EmptyState が表示、LP 全体は graceful に表示 (技術詳細出さない) — **既存 L1-S3 を維持** |
| UC-L2 | L2-S1 | 上記 §1 で再掲、変更なし |
| metadata (SEO/OGP) | L1-S5 (metadata、新規) | `<head>` の `<meta name="description">` / `<meta property="og:description">` に スタンスキーワード少なくとも 1 つが含まれる (Unit U-T3 の E2E 反映、headless で実 `<head>` を取得して assert) |

## 3. 移行検証シナリオ (マイグレーションある時)

| シナリオ ID | 移行前データ | 移行後期待状態 |
|---|---|---|
| (該当なし) | — | — |

→ DB / Config マイグレーション不要のため本セクション空。

## 4. 環境要件差分

| 項目 | 前回 | 今回 | 理由 |
|---|---|---|---|
| ブラウザ | Chromium (主) + WebKit | **変更なし** | 純文言改修、レンダラ依存性なし |
| 画面サイズ | モバイル / デスクトップ | **変更なし** | レイアウト構造不変 |
| オフライン | ❌ | ❌ (変更なし) | — |
| 認証 | 不要 (公開) | **変更なし** | — |
| HUB status | mock (`getCachedStatus` をスタブ) | **変更なし** | service-status 側に依存しない改修 |
| **Level 1 snapshot fixture** | `landing-happy.png` (旧コピー版) | **新コピーで再撮、PR 同梱** | コピー差し替えで必ず差分が出るため (前回 fixture との不一致は本改修の意図的差分) |

## 5. レイアウト・ビジュアル検証 (O34、元 004 §5 継承)

### 5.1 Level 1 (snapshot, CI)
| シナリオ | スクショ | mask | 備考 |
|---|---|---|---|
| L1-S1 | `landing-happy.png` (**新コピー版で再撮**) | 稼働日数 (動的時刻) — **変更なし** | 本改修コミットと同 PR で fixture 再撮 |

### 5.2 Level 2 (意味的)
| # | 要件 | アサーション | 備考 |
|---|---|---|---|
| L2-1 | ヒーローが最上部 | Hero.y < 稼働一覧.y | **変更なし** (構造不変) |
| L2-2 | CTA が可視・到達可 | CTA boundingBox 存在、role=link | **変更なし** |
| L2-3 | 稼働一覧が主役 (余白) | section 間 margin が design SoT スケール | **変更なし** |
| **L2-4 (新規)** | **スタンスが視認可能位置にある** | **ConsultPitch (or Hero) の領域内に「共に考え / 共に悩 / 正解の見えない / 絶対の正解」キーワード DOM ノードが存在し、ビューポート ≤ 2000px 内 visible** | スクロール極大で初めて見える設計を防止 (O41 入口理解、スタンスも入口で読めること) |

### 5.3 Level 3 (AI Vision)
- 採用: ❌ (MVP) — **変更なし** (元 004 §5.3 継承)

### 5.4 採用 Level
- Level 1 ✅ / Level 2 ✅ / Level 3 ❌ — **変更なし**

## 6. 期待 KPI

| 指標 | 目標 |
|---|---|
| シナリオ成功率 | 100% (L1-S1, L1-S2', L1-S3, L1-S4, L1-S5, L2-S1, L2-S2) |
| Level 1 差分 | 0 (本改修コミットで snapshot 再撮済を前提) |
| Level 2 pass | 100% (L2-1〜L2-4) |
| (新規) スタンスキーワード検出率 | 100% (L1-S2' で 2 種類以上 + L1-S5 で 1 種類以上) |
| (新規) アンチパターン検出率 | 0% (L1-S4 で 1 件も検出されない) |

## 7. 更新履歴

| 日付 | 変更概要 | 実行者 |
|---|---|---|
| 2026-05-28 | 初版作成 | /flow:revise |
